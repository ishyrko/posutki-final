# In all environments, the following files are loaded if they exist,
# the latter taking precedence over the former:
#
#  * .env                contains default values for the environment variables needed by the app
#  * .env.local          uncommitted file with local overrides
#  * .env.$APP_ENV       committed environment-specific defaults
#  * .env.$APP_ENV.local uncommitted environment-specific overrides
#
# Real environment variables win over .env files.
#
# DO NOT DEFINE PRODUCTION SECRETS IN THIS FILE NOR IN ANY OTHER COMMITTED FILES.
# https://symfony.com/doc/current/configuration/secrets.html
#
# Run "composer dump-env prod" to compile .env files for production use (requires symfony/flex >=1.2).
# https://symfony.com/doc/current/best_practices.html#use-environment-variables-for-infrastructure-configuration

###> symfony/framework-bundle ###
APP_ENV=dev
APP_SECRET=change_this_to_something_more_secure_in_production
TRUSTED_PROXIES=127.0.0.1,REMOTE_ADDR

###> symfony/framework-bundle ###
###< symfony/framework-bundle ###

###> doctrine/doctrine-bundle ###
DATABASE_URL="mysql://root:12345@host.docker.internal:3306/posutki_db?serverVersion=8.0&charset=utf8mb4"
###< doctrine/doctrine-bundle ###

###> nelmio/cors-bundle ###
# Локальная разработка: любой хост (posutki.test, LAN и т.д.). В проде задайте узкий origin в .env.local / на сервере.
CORS_ALLOW_ORIGIN='^https?://[^/]+(:[0-9]+)?$'
###< nelmio/cors-bundle ###

###> lexik/jwt-authentication-bundle ###
JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem
JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem
JWT_PASSPHRASE=rnb_jwt_secret_2026
###< lexik/jwt-authentication-bundle ###

# Google OAuth
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID_HERE

###> symfony/routing ###
DEFAULT_URI=http://localhost
###< symfony/routing ###

###> symfony/messenger ###
MESSENGER_TRANSPORT_DSN=doctrine://default?auto_setup=0
###< symfony/messenger ###

###> symfony/mailer ###
MAILER_DSN=smtp://mailpit:1025
MAILER_FROM="RNB.by <noreply@rnb.by>"
ADMIN_EMAILS=["info@rnb.by"]
###< symfony/mailer ###

FRONTEND_URL=http://localhost:3000

# SMS rate limits (abuse prevention). Override in .env.local if needed.
SMS_PHONE_COOLDOWN_SECONDS=60
SMS_PER_IP_DAILY_LIMIT=5
SMS_PER_IP_UNIQUE_PHONES_DAILY=3
SMS_GLOBAL_DAILY_LIMIT=300

# reCAPTCHA v2 — set RECAPTCHA_SECRET in .env.local for production.
RECAPTCHA_ENABLED=0
RECAPTCHA_SECRET=

# RocketSMS
ROCKETSMS_USERNAME=
ROCKETSMS_PASSWORD=
ROCKETSMS_SENDER=
ROCKETSMS_VERIFICATION_TEMPLATE="Ваш код подтверждения: %s"

# Next.js on-demand revalidation (POST to /revalidate on Next — not /api/revalidate)
NEXTJS_REVALIDATE_URL=
NEXTJS_REVALIDATION_SECRET=
